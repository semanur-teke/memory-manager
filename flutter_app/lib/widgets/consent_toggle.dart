/// Riza toggle switch widget'i.
/// Yesil (riza var) / gri (riza yok).
/// NOT: Kirmizi degil — riza vermemek bir hata degil, kullanicinin hakkidir.
///
/// Parametreler: hasConsent, onChanged, itemId

class ConsentToggle {
  // Widget build(BuildContext context)
}
