/// Import ilerleme widget'i.
/// Progress bar + istatistikler (aktarilan, atlanan, hata) + dosya loglari.
///
/// Parametreler: current, total, imported, skipped, errors, currentFile

class ImportProgress {
  // Widget build(BuildContext context)
}
