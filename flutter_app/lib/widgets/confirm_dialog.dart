/// Silme/cascade onay diyalogu widget'i.
/// Tehlikeli islemlerde cift onay mekanizmasi.
/// Toplu silmede TextFormField'a "SIL" yazdirma zorunlu.
///
/// Parametreler: title, message, confirmText, onConfirm, requireTextInput

class ConfirmDialog {
  // static Future<bool?> show(BuildContext context, {...})
}
