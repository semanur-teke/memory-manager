/// Istatistik karti widget'i.
/// Buyuk sayi + ikon + baslik + renk.
/// Dashboard'da 4x kullanilir (ani, olay, tekrar, tarih araligi).
///
/// Parametreler: title, value, icon, color

class StatsCard {
  // Widget build(BuildContext context)
}
