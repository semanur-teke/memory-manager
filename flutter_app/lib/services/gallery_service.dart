/// Galeri servis katmani.
/// Item listeleme, detay, thumbnail ve fullsize API cagrilari.

class GalleryService {
  // Future<ItemListResponse> getItems({int page, int size, int? year, int? month, String sort})
  // Future<Item> getItem(int itemId)
  // Future<String> getThumbnail(int itemId) → base64 string
  // Future<String> getFullsize(int itemId) → base64 string
}
