/// Olay servis katmani.
/// Event listeleme, detay, kumeleme, guncelleme API cagrilari.

class EventService {
  // Future<EventListResponse> getEvents({int page, int size})
  // Future<EventDetail> getEvent(int eventId)
  // Future<Stream<ClusterProgress>> triggerClustering()
  // Future<void> updateEvent(int eventId, {String? title, String? mainLocation, String? summary})
  // Future<void> deleteEvent(int eventId)
}
