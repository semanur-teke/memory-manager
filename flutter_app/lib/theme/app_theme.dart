/// ThemeData birlestirme.
/// AppColors + AppTypography + spacing sistemi (8px grid).

class AppTheme {
  // static ThemeData lightTheme() → acik tema
  // static ThemeData darkTheme() → koyu tema
}

class AppSpacing {
  // xs: 4, sm: 8, md: 16, lg: 24, xl: 32, xxl: 48
  // pagePadding, cardPadding, gridSpacing
}
