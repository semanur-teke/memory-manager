/// Event veri modeli.
/// API'den gelen olay verisini Dart nesnesine donusturur.
///
/// Alanlar: eventId, title, startDate, endDate, mainLocation,
///          summary, coverItemId, itemCount

class Event {
  // factory Event.fromJson(Map<String, dynamic> json)
  // Map<String, dynamic> toJson()
}
