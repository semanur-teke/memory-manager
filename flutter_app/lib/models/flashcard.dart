/// Flashcard veri modeli.
///
/// Alanlar: flashcardId, question, answer, eventId,
///          eventTitle, relatedItemIds, nextReviewDate

class Flashcard {
  // factory Flashcard.fromJson(Map<String, dynamic> json)
  // Map<String, dynamic> toJson()
}
