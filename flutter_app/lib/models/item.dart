/// Item veri modeli.
/// API'den gelen item verisini Dart nesnesine donusturur.
///
/// Alanlar: id, filePath, fileHash, type, hasConsent, isRotated,
///          creationDatetime, latitude, longitude, transcription,
///          faissIndexId, eventId

class Item {
  // factory Item.fromJson(Map<String, dynamic> json)
  // Map<String, dynamic> toJson()
}
