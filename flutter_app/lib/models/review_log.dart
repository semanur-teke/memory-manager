/// ReviewLog veri modeli.
/// SM-2 tekrar loglari.
///
/// Alanlar: logId, flashcardId, reviewDate, userRating, nextReviewDate

class ReviewLog {
  // factory ReviewLog.fromJson(Map<String, dynamic> json)
  // Map<String, dynamic> toJson()
}
