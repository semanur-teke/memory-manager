/// Arama ekrani.
/// Semantik metin + zaman + konum bazli birlesik arama.
///
/// API Cagrilari:
///   POST /api/search
///   POST /api/search/advanced
///
/// Filtre kombinasyonlari:
///   - Sadece metin → CLIP + SBERT
///   - Metin + tarih → semantik + zaman kesisimi
///   - Metin + sehir → semantik + konum kesisimi
///   - Tumu → uclu kesisim

class SearchScreen {
  // Widget build(BuildContext context)
}
