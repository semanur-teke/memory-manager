/// Fotograf detay ekrani.
/// Tam boyut foto, metadata, riza toggle, silme.
///
/// API Cagrilari:
///   GET /api/items/{id}
///   GET /api/items/{id}/fullsize
///
/// Kritik UX:
///   - InteractiveViewer ile zoom in/out
///   - Silme → ConfirmDialog
///   - Riza kaldirma → ek onay

class PhotoDetailScreen {
  // Widget build(BuildContext context)
}
