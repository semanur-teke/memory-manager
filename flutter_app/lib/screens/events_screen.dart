/// Olaylar listesi ekrani.
/// Olay bazli gruplama gorunumu, kapak fotografi, yeniden kumeleme.
///
/// API Cagrilari:
///   GET /api/events?page=1&size=20
///   POST /api/events/cluster (yeniden kumeleme)

class EventsScreen {
  // Widget build(BuildContext context)
}
