/// Fotograf / ses ice aktarma ekrani.
/// Klasor secimi, riza checkbox, SSE ile gercek zamanli ilerleme.
///
/// API Cagrilari:
///   POST /api/import/folder (SSE stream)
///
/// Kritik UX:
///   - Riza checkbox isaretlenmeden buton DISABLED
///   - SSE ile gercek zamanli progress bar
///   - Import sirasinda "Iptal" butonu
///   - Tamamlandiginda "Galeriye Git" butonu

class ImportScreen {
  // Widget build(BuildContext context)
}
