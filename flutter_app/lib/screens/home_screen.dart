/// Dashboard ekrani.
/// Toplam ani, olay sayisi, bugunun tekrar kartlari, son eklenen fotograflar.
///
/// API Cagrilari:
///   GET /api/timeline/stats
///   GET /api/items?size=12&sort=desc
///   GET /api/events (count)
///   GET /api/flashcards/due-today (count)
///
/// Widget'lar: 4x StatsCard, GridView (12 EncryptedImage), TextField, 3x ElevatedButton

class HomeScreen {
  // Widget build(BuildContext context)
}
