/// Arama state management (Riverpod).
/// SearchState: query, results, isLoading, filtersApplied

class SearchState {
  // final String query
  // final List<SearchResult> imageResults
  // final List<SearchResult> textResults
  // final bool isLoading
  // final Map<String, bool> filtersApplied
}

class SearchNotifier {
  // Future<void> search(String query, {DateTime? startDate, DateTime? endDate, double? lat, double? lng, double? radiusKm})
  // Future<void> advancedSearch({String? query, int? year, int? month, String? city})
  // void clearResults()
}
