/// Flashcard state management (Riverpod).
/// FlashcardState: allCards, dueToday, currentIndex, isLoading

class FlashcardState {
  // final List<Flashcard> allCards
  // final List<Flashcard> dueToday
  // final int currentIndex
  // final bool isLoading
}

class FlashcardNotifier {
  // Future<void> loadAllCards()
  // Future<void> loadDueToday()
  // Future<void> submitReview(int flashcardId, int rating)
  // void nextCard()
  // void previousCard()
}
