/// Olay state management (Riverpod).
/// EventState: events, isLoading, isClustering, clusterProgress

class EventState {
  // final List<Event> events
  // final bool isLoading
  // final bool isClustering
  // final double clusterProgress
}

class EventNotifier {
  // Future<void> loadEvents({int page, int size})
  // Future<void> triggerClustering()
  // Future<void> updateEvent(int eventId, {String? title, String? mainLocation, String? summary})
  // Future<void> deleteEvent(int eventId)
}
